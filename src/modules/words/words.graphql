type Word {
  _id: ID!
  name: String!
  category: Category!
}

type Query {
  words: [Word!]!
}

type Mutation {
  createWord(data: WordCreateInput!): Word!
}

input WordCreateInput {
  name: String!
  categoryId: ID!
}
